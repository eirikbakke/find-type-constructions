{
  "name": "find-type-constructions",
  "displayName": "Find Type Constructions",
  "description": "Find all object literals assignable to the interface or type under the cursor.",
  "version": "0.0.1",
  "publisher": "local",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "find-type-constructions.find",
        "title": "Find Type Constructions"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "find-type-constructions.find",
          "when": "editorLangId == typescript || editorLangId == typescriptreact",
          "group": "navigation@100"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.4.0"
  }
}
